# .github/workflows/keep-alive.yml
name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 8 */3 * *'  # Se ejecuta cada 3 dias a las 8am UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl https://iagcjhepljtsnjoiivkm.supabase.co/auth/v1/health
